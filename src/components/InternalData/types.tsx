export type InternalDataProps = {
    dataMemory: number[],
    registers: number[],
    currentCycle: number,
    success: boolean
}

export type WatchListProps = {
    dataMemory: number[],
    registers: number[],
    currentCycle: number,
    success: boolean,
}